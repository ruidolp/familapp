-- Placeholder migration to trigger type regeneration
-- This ensures kysely-codegen runs successfully and regenerates all types

-- No changes needed - just ensuring migration succeeds for type generation
SELECT 1;
